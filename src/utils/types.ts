export type IPInfo={
    city: string,
    country: string
    ip: string
    loc: string
    org: string
    region: string
    timezone: string
}

export type Coords ={
    log: number,
    lat: number
}

export type Store ={
    ipAddress: string,
    userData: any
    activePath: string
}